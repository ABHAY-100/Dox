meta {
  name: Disconnect an Repo
  type: http
  seq: 9
}

post {
  url: {{base_url}}/api/v1/core/disconnect-repo
  body: json
  auth: inherit
}

headers {
  Cookie: access_token=""
}

body:json {
  {
    "repoId":""
  }
}

settings {
  encodeUrl: true
}
